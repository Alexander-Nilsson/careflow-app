image: node:14

stages:
  - test

test:
  stage: test
  script:
    - cd careflow-app # Change the directory path accordingly
    - ls
    - npm install # Install dependencies
    - npm test # Run tests
